<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ScreenActivity"
    android:background="@drawable/sky_pic">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/image_view_screen_sun"
            android:layout_width="@dimen/sun_logo_size"
            android:layout_height="@dimen/sun_logo_size"
            android:src="@drawable/sun" />

        <TextView
            android:id="@+id/text_view_screen_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/weather_app"
            android:layout_gravity="center"
            android:textSize="@dimen/screen_app_name_size"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:layout_margin="@dimen/standard_margin"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>